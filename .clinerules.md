CRITICAL SYSTEM INSTRUCTIONS (MUST READ BEFORE ACTIONS)
1. CONTEXT & LIFECYCLE RULES
BEFORE performing any task or replying to the user, you MUST read, analyze, and strictly follow the instructions, commands, and project state defined in CLAUDE.md.

Keep CLAUDE.md updated as the project progresses.

2. UI DEVELOPMENT & MODIFICATION RULES
EVERY SINGLE TIME you receive a request to design, modify, fix, or update the User Interface (UI), you MUST read and fully comply with the design tokens, CSS variables, and utility classes specified in the project's global CSS file (e.g., DesignUI.css).

Strict Reuse: You MUST directly use the pre-defined utility classes (e.g., .glass, .btn-primary, .card, .input) and CSS variables (e.g., var(--soft-periwinkle), var(--radius-md)) from that CSS file to construct the UI.

Do not invent new UI styles, inline styles, or arbitrary Tailwind/CSS classes that contradict or duplicate the established design system in the CSS file.

3. STEERING DRIFT CHECK
Before committing code that changes API contracts, routes, database schema, shared hooks, shared UI primitives, or architecture notes, run:

python scripts/check_steering_drift.py --mode all

The repository also uses .githooks/pre-commit via core.hooksPath to warn when staged changes likely require .kiro/steering review. The hook is advisory and must not be treated as a replacement for manually updating steering when behavior changes.
